name: ice
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      # Core scientific computing
      - numpy>=2.0.0
      - scipy>=1.10.0
      - pandas>=2.0.0
      # Machine learning
      - scikit-learn>=1.3.0
      # Visualization
      - matplotlib>=3.5.0
      - seaborn>=0.11.0
      # Testing
      - pytest>=7.0.0
